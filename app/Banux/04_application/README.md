# Product application

Place product-specific orchestration and Banux application callbacks in this
directory. Reusable product features belong in `03_application_components`;
reusable framework services belong in `02_system_components`.
